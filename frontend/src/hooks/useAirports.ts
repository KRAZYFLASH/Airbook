// Airport interface for type definitions

export interface Airport {
  id: string;
  name: string;
  city: string;
  country: string;
  airport: string;
  code: string;
  description?: string;
  imageUrl?: string;
}
